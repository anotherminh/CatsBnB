<h1>Cat Society</h1>

<p> <%= link_to "Create a New Cat", new_cat_url %></p>

<ul>
  <% @cats.each do |cat| %>
    <li>
      <a href="<%= cat_url(cat) %>"><%= cat.name %></a>
    </li>
  <% end %>
</ul>
